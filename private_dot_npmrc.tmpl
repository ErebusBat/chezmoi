{{- if eq .focus "tractionguest" }}
@tractionguest:registry=https://npm.pkg.github.com/
//npm.pkg.github.com/:_authToken={{- (gopassRaw "tguest/tokens" | mustFromJson).github }}
@xlts.dev:registry=https://registry.xlts.dev/
always-auth=true
//registry.xlts.dev/:always-auth=true
//registry.xlts.dev/:_authToken={{- (gopassRaw "tguest/tokens" | mustFromJson).xlts }}
{{- else if eq .focus "lshq" -}}
//npm.pkg.github.com/:_authToken={{- (gopassRaw "lshq/tokens" | mustFromJson).github }}
{{ end -}}

//registry.npmjs.org/:_authToken={{- (gopassRaw "erebusbat/tokens" | mustFromJson).npm_registry -}}

