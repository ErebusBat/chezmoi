" eXtra file for s_colors.vim session
" The filename is IMPORTANT as it is auto-invoked by vim when loading the colors
" session (see :h mksession)

" Override session hotkey to save to correct place
nnoremap <Leader>ss :mksession! ~/.config/nvim/s_colors.vim<CR>
