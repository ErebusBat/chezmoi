#!/bin/zsh
REPO=${REPO:-${1}}
REPO=${REPO:-erebusbat_yellowsea}
# {{ .chezmoi.hostname }}

# FreeNAS Access Info, Not Host specific
export AWS_ACCESS_KEY_ID={{- (gopassRaw "restic/FreeNAS_Minio_S3" | mustFromJson).key_id }}
export AWS_SECRET_ACCESS_KEY={{- (gopassRaw "restic/FreeNAS_Minio_S3" | mustFromJson).secret }}
# export AWS_ACCESS_KEY_ID=Gqat1pibfccPGWET4eL6
# export AWS_SECRET_ACCESS_KEY=yqvPHhlRITgU82lXpS4A
# restic --repo=s3:http://nas.erebusbat.net:9000/restic/backrest/erebusbat --no-lock --password-file ~/.restic/.erebusbat.key $*
